{
  "pluginAlias": "LgEss",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "LG ESS"
      },
      "essIp": {
        "title": "ESS IP Address",
        "type": "string",
        "required": true,
        "description": "Local IP address of the LG ESS inverter (e.g. 192.168.1.50)",
        "format": "ipv4"
      },
      "essPassword": {
        "title": "ESS Password",
        "type": "string",
        "required": true,
        "description": "Password for the LG ESS (usually the MAC address of the LAN interface without colons)"
      },
      "pollingInterval": {
        "title": "Polling Interval (seconds)",
        "type": "integer",
        "required": false,
        "default": 10,
        "minimum": 5,
        "maximum": 300,
        "description": "How often to poll the ESS for updated data"
      }
    }
  }
}
